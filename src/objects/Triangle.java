package objects;

import model.Solid;
import model.Vertex;
import transforms.Point3D;

public class Triangle extends Solid {
    public Triangle(){
        getGeometry().getVertecies().add(new Vertex(new Point3D(0,0,0), ))
    }
}
